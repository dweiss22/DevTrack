"use client";
import { useState } from "react";
export default function LoginPage() { const [email, setEmail] = useState(""); const [notice, setNotice] = useState(""); return <main className="login"><section className="card"><p className="eyebrow">DEVTRACK</p><h1>Sign in</h1><p>Use your approved organization email to receive a secure sign-in link.</p><form onSubmit={async (event) => { event.preventDefault(); const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); const body = await response.json(); setNotice(response.ok ? "Check your email for a sign-in link." : body.error); }}><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" required /><button>Send sign-in link</button></form>{notice && <p className="notice">{notice}</p>}</section></main>; }
