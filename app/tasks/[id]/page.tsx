import { notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { requireContext } from "@/lib/auth";
import { hours } from "@/lib/metrics";

export default async function TaskDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const { supabase, profile } = await requireContext(); const { data: task } = await supabase.from("wrike_tasks").select("*,wrike_time_entries(entry_date,minutes,comment,wrike_users(display_name,email)),wrike_task_assignees(wrike_users(display_name))").eq("id", id).eq("organization_id", profile.organization_id).maybeSingle(); if (!task) notFound(); const row = task as unknown as { title: string; status: string; description: string | null; permalink: string | null; due_date: string | null; completed_at: string | null; planned_minutes: number | null; raw_data: unknown; wrike_time_entries: { entry_date: string; minutes: number; comment: string | null; wrike_users: { display_name: string; email: string | null } | null }[]; wrike_task_assignees: { wrike_users: { display_name: string } | null }[] }; return <AppShell><header className="page-header"><div><p className="eyebrow">TASK DETAIL</p><h1>{row.title}</h1><p>{row.status} · Due {row.due_date ?? "not set"}</p></div>{row.permalink && <a className="button" href={row.permalink} target="_blank">Open in Wrike</a>}</header><div className="admin-grid"><section className="card"><h2>Reporting details</h2><p><strong>Assignees:</strong> {row.wrike_task_assignees.map((item) => item.wrike_users?.display_name).filter(Boolean).join(", ") || "Unassigned"}</p><p><strong>Planned effort:</strong> {row.planned_minutes == null ? "Not available" : `${hours(row.planned_minutes)} hours`}</p><p><strong>Completion:</strong> {row.completed_at ? new Date(row.completed_at).toLocaleString() : "Not completed"}</p><p>{row.description || "No task description supplied by Wrike."}</p></section><section className="card"><h2>Original Wrike metadata</h2><pre>{JSON.stringify(row.raw_data, null, 2)}</pre></section></div><section className="card"><h2>Time entries</h2>{row.wrike_time_entries.length ? <table><thead><tr><th>Date</th><th>Person</th><th>Hours</th><th>Comment</th></tr></thead><tbody>{row.wrike_time_entries.map((entry, index) => <tr key={index}><td>{entry.entry_date}</td><td>{entry.wrike_users?.display_name ?? "Unknown"}</td><td>{hours(entry.minutes)}</td><td>{entry.comment ?? "—"}</td></tr>)}</tbody></table> : <p className="empty">No recorded time for this task.</p>}</section></AppShell>; }
