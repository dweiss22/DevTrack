export default function DevelopmentLoading() { return <><header className="page-header"><div><p className="eyebrow">DEVELOPMENT</p><h1>Loading course-development dashboard</h1></div></header><div className="development-loading" aria-busy="true">{Array.from({length:3},(_,index)=><section className="card loading-chart loading-pulse" key={index}><span className="sr-only">Loading section {index+1}</span></section>)}</div></>; }
